<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="../public/css/tailwind-output.css" rel="stylesheet" />
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto p-4">
      <div class="relative flex flex-col md:flex-row">
        <!-- Include Sidebar -->
        <%- include('../components/sidebar'); %>

        <div class="ml-0 md:ml-64 flex flex-col w-full">
          <!-- Include Navbar -->
          <%- include('../components/navbar'); %>

          <div class="content p-4 flex justify-center">
            <div
              class="content p-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"
              id="pharmacyContainer"
            >
              <!-- Cards will be dynamically added here -->
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="../public/js/pharmacy.js"></script>
  </body>
</html>
